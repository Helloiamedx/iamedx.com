# Pre-SpecularButton backup

Snapshot of hero/nav CTA wrappers before SpecularButton (edge specular) wiring.

- HeroActions.tsx — View services + Contact with GlareHover + ClickSpark
- Header.tsx — full header including nav Contact GlareHover
- GlareHover.* — glare effect used on those CTAs

Delete this folder once SpecularButton is kept.

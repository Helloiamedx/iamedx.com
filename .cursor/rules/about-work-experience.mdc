---
description: Edward work experience — full bullets live in content/experience.ts; About Experience is a temporary career strip until a dedicated Experience UI is designed.
alwaysApply: false
globs: content/experience.ts,app/about/**
---

# About work experience

## Source of truth

Full roles + bullet points: `content/experience.ts` (`experienceRoles`).

Do **not** invent bullets or dates. When the user redesigns Experience / career presentation, pull from that file.

## Current UI (temporary)

About page **Experience** (`#experience`) lists career rows only:

1. Index (`01`…)
2. Role (large)
3. Company (logo slot later via `logo`)
4. Country (`country` — Title Case in data; uppercase in UI)
5. Period

- No “All projects” / “Selected Work” label
- Section label sticky on the left (same grid language as About)
- Not project case studies

## Country copy

Use polished country names in `experienceRoles[].country` (e.g. `United States`, not “unit states”). Do not invent countries.

## When logos arrive

Wire each role’s `logo` in `experienceRoles` and render in `.about-boua__work-company`.

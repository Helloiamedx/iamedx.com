---
description: Projects index showcase + case detail template (Pentagram-style AboutCaseDemo)
globs: app/projects/**/*.{tsx,ts,css}, app/about/**/*.{tsx,ts}, components/Project*.tsx, components/AboutCaseDemo.tsx, content/projects.ts, content/caseCopy.ts, app/globals.css
alwaysApply: false
---

# Projects layout

## Case detail template (`/projects/[slug]`) — STANDARD

Living reference: [`components/AboutCaseDemo.tsx`](components/AboutCaseDemo.tsx) + `.about-case-demo*` in [`app/globals.css`](app/globals.css) (mounted on `/projects/[slug]`; `/about` is bio-only).

This is the **only** case-detail pattern. Do not revive the old always-on Background / Challenges / Execution / Impact chapter stack on detail pages.

### 1. Full-viewport video hero

- Height `100svh` / `100vh`, pulled under sticky header (`margin-top: -header-offset`).
- Full-bleed media (site video / ProtectedVideo or project hero source).
- Overlay copy **bottom-left**, shell-aligned: eyebrow (optional) · title · short summary · tags.
- Nav at rest: clear / over-clear on the video (same as home).

### 2. Image gallery (content band)

- Shell width: `min(var(--shell-max), calc(100% - var(--shell-gutter) * 2))` — **do not invent local outer padding**.
- Vertical stack of full-width frames + 2-up pairs.
- Row gap between blocks = `--shell-gutter`.
- Pair (side-by-side) gap tighter: `calc(var(--shell-gutter) * 0.35)` from `800px` up.
- Images are **display-only** (no lightbox) unless a project explicitly asks otherwise.

### 3. “About the project” control

- Shell-fixed (portaled), **not** parented inside the media column — open-push must not move X.
- Activity band = real media stack (and when open, taller of media vs copy). Never over the video; release at content bottom.
- Travels with content top → docks at `header-offset + shell-gutter` → clamps at content bottom.
- Font weight **Medium (500)**.
- Desktop scroll uses **Lenis** (`lenis.scrollTo`, `immediate: true`); `window.scrollTo` alone is not enough.

### 4. About panel (toggle)

- Closed: images full width; panel collapsed (`height: 0`) so hidden copy does **not** inflate gallery height.
- Open: media ~60% / panel ~40%; media gets `padding-right: var(--shell-gutter)` (image↔copy gap).
- Panel slides in; media width eases (push, not hard snap).
- Copy + meta flow with images (**no sticky drift**) so tops stay aligned.
- On open: jump so **copy line 1** is under the header (Lenis immediate scroll).
- Mobile: full-screen overlay panel + body scroll lock; keep a Close control.

### 5. Copy / type

- Apple-style hierarchy: titles `#1d1d1f` / body `#6e6e73`; title and body ~two steps above the old 14px stack (`1.375rem` / `1.125rem` desktop).
- Separate heading + paragraph blocks with clear title→body gap (not `<strong>` + `<br>` in one `<p>`).
- Structure: project narrative blocks + meta (Client / Sector / Discipline / Partners / team as data allows).

---

## Cover ratios (index)

1. **Featured lead (first)** — `1920 × 1080` (exact 16:9).
2. **Two-column cards** — `3000 × 1687` (~16:9; use `aspect-ratio: 3000 / 1687`).

## Featured lead meta (full width)

Further-style single row: **title left** · **tagline center** · **tags right** (` · ` separators).

## Two-column card meta (match reference)

Under the image:

```
TITLE (left, bold/heavy)          TAGS (right, light, comma-separated)
Tagline under the title (left)
```

Not a three-column horizontal bar. Title and tags share the top baseline; copy sits under the title.

These fields feed the filter system later — keep as structured data.

## Page arrangement (`/projects`)

1. **First project** — full width of the menu/`shell`.
2. **Everything after** — **two equal-width columns**. Same meta stack on every card.

## Do not

- Return to irregular masonry / auto-fill multi-column grids for the showcase
- Mix the featured three-column meta into two-column cards (or vice versa)
- Use the old four-chapter (Background → Challenges → Execution → Impact) stack as the case detail template
- Parent the About button inside the media column (it will slide left on open)
- Leave closed About copy in normal flow (it inflates height and breaks the button band)
- Change site `--shell-gutter` / outer shell padding for local “tweaks” on the case template

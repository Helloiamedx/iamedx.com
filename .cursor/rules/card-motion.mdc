---
description: 卡片出场动效调用规则（Apple 出场；C01–C06 仅存档）
globs: components/**/*.tsx,app/globals.css,lib/appleCardReveal.ts
alwaysApply: true
---

# 卡片出场动效调用协议

站点当前只有**一套**卡片出场系统：**Apple 出场**（`lib/appleCardReveal.ts`）。

另有 C01–C06 六种效果的**原始脚本存档**在 `backups/card-motion/`，**未接入站点**。
早期有过一份移植实现（`lib/cardMotion.ts`），在首页三个 band 改用 Apple 出场后
已被用户要求删除 —— **不要再 import 它**；若以后要恢复 C 系列，按下面 C 系列一节
的参数表重新移植，不要改参数。

## Apple 出场（当前唯一接线中的系统）

来源：用户提供的 Apple 页面脚本（`lib/appleCardReveal.ts` 逐字移植机制）。
参数为原作者值，**不要**改：错峰 `150ms` · 上移 `30px` · 位移 `700ms` ·
淡入 `900ms` · 触发「卡片顶部到达视口高度 `85%`」· 缓动 `easeInOutQuad`。

```tsx
import { useAppleCardReveal } from "@/lib/appleCardReveal";

useAppleCardReveal(sectionRef, ".my-card");                      // 位移 + 淡入
useAppleCardReveal(sectionRef, ".home-recognition__card", { fade: false });
```

- 逐帧 `requestAnimationFrame` 写内联 `opacity` / `transform` / `will-change`；
  挂载时记录原始内联值，settle / destroy 时**原样归还**。
- 挂载期间给根节点加 `data-apple-reveal`、给卡片加 `data-reveal-item`；
  `app/globals.css` 里 `.is-revealing [data-reveal-item] { transition: none !important }`
  负责屏蔽卡片自带的过渡（否则每帧都会被过渡插值，位移会变黏）。
- 触发按**每一张卡片自己**判断：卡顶过 `85%` 视口高才播。不要改成整组判断 ——
  首页项目 / 见解网格是三行以上的长列表，整组触发会在首行刚进视口时把整列一次跑完，
  下面几行在看不见的地方就播完了。**同一行**的卡片才彼此错峰（左 → 右 0 / 150 / 300ms），
  行的划分按实际布局（`getBoundingClientRect`）读取，所以二列 / 一列重排后依然正确。
  单行轮播（Recognition / My approach）行为不变：五张卡同属一行，仍然是 0/150/300 级联。
- 挂载**之后**才出现的卡片也会被接上：模块用 `MutationObserver` 监听根节点，
  新卡自动进入待播队列（项目页 “See more projects” 展开、以及筛选切换都是这个路径）。
  为此 `useAppleCardReveal` 会把 `selector` 一起传进引擎 —— 去掉它新卡就不会出场。
- `fade: false` 用于透明度归 CSS 管的卡片。

## 已接线的卡片（当前）

| 位置 | 选择器 | 效果 |
| --- | --- | --- |
| 首页 `Recognition comes first.` | `.home-recognition__card` | Apple 出场 · `fade: false` |
| 首页 `What makes me stand out.` | `.home-different__feature` | Apple 出场 |
| 首页 `My approach.` | `.home-someone__slide` | Apple 出场（只做纵向位移，不干扰轮播横向测量） |
| 首页 Products / Thoughts 卡片 | `.project-showcase__item` / `.insight-showcase__item` | Apple 出场（同组 3 张） |
| `/projects` 网格 | `.project-showcase__item` | Apple 出场（含 “See more projects” 新增卡） |
| `/thoughts` 网格 | `.insight-showcase__item` | Apple 出场（筛选切换后重挂载重播） |

除轮播三处外，其余全部走 `components/CardRevealGroup.tsx` 包装 —— 它把出场限定在
指定页面，不污染共享的 `ProjectMasonry` / `InsightMasonry`。

**Recognition 用 `fade: false` 的原因**：轮播把非当前卡片压到 `opacity: .55`；
若统一淡入到 1，结束释放控制权时会回弹。这批卡片透明度交给 CSS，只做位移。

## C 系列（存档，未接线）

六种效果的原始实现逐字保留在 `backups/card-motion/card-entrance.js`，
说明见 `backups/card-motion/README.md`。**站点当前没有它的实现。**

| 编号 | 名称 | 参数 |
| --- | --- | --- |
| C01 | 整组轻升 | 同时上移 28px · 1000ms（不错开） |
| C02 | 错峰上浮 | 依次上移 36px · 1000ms · 间隔 120ms（默认） |
| C03 | 缩放落定 | `scale(.94) translateY(12px)` → 归位 · 1150ms · 间隔 100ms |
| C04 | 横向滑入 | 从右 `translateX(56px)` → 0 · 1050ms · 间隔 130ms |
| C05 | 微倾归位 | `translateY(30px) rotateX(7deg) scale(.98)` → 归零 · 1200ms · 间隔 120ms |
| C06 | 卡片先行 | 卡片 24px/900ms；内容延后 +220ms（14px/800ms）；插画延后 +300ms（blur 7→0/1000ms） |

公共参数：基础延迟 `140ms`；缓动 `cubic-bezier(.22,1,.36,1)`；`fill: 'both'`；
`speed` 0.5×–1.5× 同时作用于 `duration` 与 `delay`；尊重 `prefers-reduced-motion`。

## 触发时机（C 系列移植时的注意点）

C 系列按**卡片本身**判断视口（默认 `amount: 0.3`），不是 section 级 threshold。
原因：首页 hero 占满首屏，整段 band 的上边缘在页面一加载就贴住视口底部；
若按 section 级别判断，动画会在后台提前播完，用户滚到时已经结束。
Apple 出场不用这套（整组判断，见上）。

## 移植要点（C 系列）

原始脚本依赖 ChatGPT 小组件环境，接入站点时必须处理：

1. 删掉 `window.openai.setWidgetState` / `widgetState` / `openai:set_globals`
   （ChatGPT Apps SDK 状态持久化）——换成 React state 或 sessionStorage。
2. 删掉 `globalThis.Tweak` 分支（ChatGPT 调试面板滑杆）。
3. `#card-motion` / `.cm-card` / `.cm-text` / `.cm-art` / `.cm-choice` 等类名
   换成目标组件自己的结构。
4. React 场景：在 effect 里创建、卸载时 `cancel()` 全部动画；不要在父级额外加 key。

## 不要

- 把 C 编号和标题动效的 `01`–`07` 混用（两套独立协议）
- 把 Apple 出场和 C 系列混用（两套独立协议，各自参数不得互改）
- import 已删除的 `lib/cardMotion.ts`（要 C 系列就按本文档重新移植）
- 改参数、改缓动、改编号映射
- 在未确认目标组件结构前直接照抄类名

## 相关

- 标题出场（`01`–`07`）：`lib/headlineMotion.ts` + `components/HeadlineMotion.tsx`，
  见 `.cursor/rules/headline-motion.mdc`
- Apple 出场：`lib/appleCardReveal.ts` + `app/globals.css` 里的 `.is-revealing` 过渡屏蔽
- C 系列（存档）：`backups/card-motion/`

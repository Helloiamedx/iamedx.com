---
description: 改完就报，不要自己验证 —— 验证由用户本人完成
alwaysApply: true
---

# 不要自行验证

用户本人会验证结果。**改完代码直接汇报，不要自己跑验证。**

## 不要做

- 不要打开 / 操作浏览器（`browser_navigate`、`browser_take_screenshot`、`browser_cdp`、点击、滚动、截图）
- 不要用 CDP / DevTools 探页面（计算样式、合成事件、尺寸测量、性能采样）
- 不要 dispatch 合成 `wheel` / `pointer` / `touch` 事件去模拟手势
- 不要为了「确认效果」去起 dev server、curl 页面、跑 e2e / 单元测试
- 不要把「验证结果」表格、验证截图、`tsc` / `eslint` 输出当作交付内容
- 不要为了自检而反复重跑构建

## 要做的

1. 按用户要求把改动落到 `main`。
2. 直接说明**改了什么、改了哪个文件、为什么这么改**。
3. 不确定的地方用文字讲清取舍，把判断权交给用户。
4. 改完后停下来等用户反馈，不要自作主张继续「顺手再验一下」。

## 例外

只有当用户**明确要求**「去验证 / 跑一下 / 截图给我看」时，才执行对应动作。
一次授权只对应一次请求，不要延伸到后续改动。
